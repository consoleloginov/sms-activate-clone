export type CatalogItem = {
  id: string
  name: string
  minPrice: number
  logo_url: string
}

export type CatalogCountryItem = {
  id: string
  name: string
  price: number
  quantity: number
  flag_url: string
}
