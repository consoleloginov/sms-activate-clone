export type CatalogItem = {
  id: string
  name: string
  logo_url: string
  min_price: number
}

export type CatalogCountryItem = {
  id: string
  name: string
  price: number
  quantity: number
  flag_url: string
}
