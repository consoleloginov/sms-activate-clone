import type {CatalogItem} from './types'

export const useCatalogFavoritesCUD = () => {
  const add = async (item: CatalogItem) => {

  }

  const upd = async (item: CatalogItem) => {

  }

  const del = async (item: CatalogItem) => {

  }

  return {
    add,
    del,
    upd,
  }
}
